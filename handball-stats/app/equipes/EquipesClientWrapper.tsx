"use client";
import dynamic from "next/dynamic";

// Suppression du wrapper scraping, déplacé vers la page des compétitions
export default function EquipesClientWrapper({ equipes }: { equipes: any[] }) {
  return null;
}
